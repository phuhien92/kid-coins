import { ParentShellPrototype } from "@/components/parent/prototype/ParentShellPrototype";

/**
 * PROTOTYPE (HIE-17) — Parent dashboard shell variations.
 * Run: pnpm dev → /parent/prototype-shell
 * See: src/components/parent/prototype/NOTES.md
 */
export default function ParentShellPrototypePage() {
  return <ParentShellPrototype />;
}
